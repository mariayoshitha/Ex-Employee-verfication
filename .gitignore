# Secrets — NEVER commit (file must live outside public_html)
tt-credentials.php
verify-techtiera-credentials.php

# Real employee data / PII — keep out of version control
data.json
data.json.bak
audit.json
audit-*.json
tt-config.json
tt-config.json.lock

# Server + tooling artifacts
error_log
.gstack/
*.zip

# Dev scratch (card-variant mockups, ad-hoc test scripts)
preview-result.html
test-*.ps1
test-*.csv
test-*.xlsx
test-*.php
